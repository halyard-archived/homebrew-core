item do
  expected do
    github_tag
    repo 'freedesktop/makedepend'
    regex /^makedepend-([\d.]+)$/
    filter /^makedepend-([\d.]+)$/
  end

  actual do
    homebrew_formula
    name 'makedepend'
  end
end
