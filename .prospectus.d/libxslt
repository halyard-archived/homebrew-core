item do
  expected do
    url_xpath
    url 'https://git.gnome.org/browse/libxslt/refs/tags'
    xpath '//*[@id="cgit"]/div/table/tr[2]/td[1]/a'
    regex /^v([\w.-]+)$/
  end

  actual do
    homebrew_formula
    name 'libxslt'
  end
end
