item do
  expected do
    url_xpath
    url 'http://www.simplesystems.org/libtiff/'
    xpath '/html/body/table/tr[3]/td/a'
    regex /^v([\d.]+)$/
  end

  actual do
    homebrew_formula
    name 'libtiff-halyard'
  end
end
