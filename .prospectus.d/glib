item do
  expected do
    gitlab_tag
    repo 'GNOME/glib'
    endpoint 'https://gitlab.gnome.org'
    regex /^(\d+\.\d*[02468]\.\d+)$/
  end

  actual do
    homebrew_formula
    name 'glib'
  end
end
